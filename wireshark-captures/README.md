# Place Wireshark packet capture files (.pcapng) in this directory.
#
# Suggested filenames:
#   syn_flood_capture.pcapng   – capture of the SYN flood simulation
#   http_flood_capture.pcapng  – capture of the HTTP flood simulation
#
# Note: *.pcapng files are excluded from version control via .gitignore
# because they can be very large binary files.  Share them via another
# channel (e.g., a shared drive link) if needed.
